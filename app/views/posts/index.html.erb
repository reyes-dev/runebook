<h1>Timeline</h1>

<%= form_with model: @post do |post| %>
  <%= post.label :body, "What's on your mind?" %><br />
  <%= post.text_area :body, size: "40x10" %>
  <%= post.submit "Create" %>
<% end %>

<ul>
  <% @posts.each do |post| %>
    <li><%= post.body %></li>
  <% end %>
</ul>